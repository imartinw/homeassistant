stages:
    - deploy

deploy_www:
    stage: deploy
    tags:
        - homeassistant
    when: always
    only:
        - master
    allow_failure: false
    script:
        - cd /usr/local/docker/homeassistant/
        - git pull origin master

