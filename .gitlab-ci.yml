stages:
    - deploy

deploy_ha:
    stage: deploy
    tags:
        - homeassistant
    when: always
    only:
        - main
    allow_failure: false
    script:
        - cd /usr/local/docker/homeassistant/
        - git pull origin main

