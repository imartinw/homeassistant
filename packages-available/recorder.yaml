#
# Recorder
#

recorder:
  auto_purge: true
  purge_keep_days: 8
  exclude:
    entity_globs:
      - binary_sensor.bren_*
      - binary_sensor.martin*
      - camera.*
      - group.*
      - sensor.bren_*
      - sensor.martin_*

