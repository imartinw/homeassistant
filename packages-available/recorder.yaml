#
# Recorder
#

recorder:
  auto_purge: true
  purge_keep_days: 8
  exclude:
    entity_globs:
      - binary_sensor.bren_*
      - binary_sensor.phone_*
      - camera.*
      - group.*
      - sensor.bren_*
      - sensor.phone_*

